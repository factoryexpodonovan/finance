<% if @finance_customer.errors.any? %>
  <div id="error_explanation">
    <div class="alert alert-error">
      The form contains <%= pluralize(@finance_customer.errors.count, "error") %>.
    </div>
    <ul>
    <% @finance_customer.errors.full_messages.each do |msg| %>
      <li>* <%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>