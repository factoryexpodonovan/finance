<h1>Listing allowed_states</h1>

<table>
  <tr>
    <th>Customer</th>
    <th>State</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @allowed_states.each do |allowed_state| %>
  <tr>
    <td><%= allowed_state.customer_id %></td>
    <td><%= allowed_state.state_id %></td>
    <td><%= link_to 'Show', allowed_state %></td>
    <td><%= link_to 'Edit', edit_allowed_state_path(allowed_state) %></td>
    <td><%= link_to 'Destroy', allowed_state, method: :delete, data: { confirm: 'Are you sure?' } %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New Allowed state', new_allowed_state_path %>
